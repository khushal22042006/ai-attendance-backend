# ha-backend

Minimal README to satisfy Poetry `readme` setting.

Run the server:

```powershell
Set-Location -LiteralPath "C:\Projects\python web tutorial\ai-attendance-backend\backend\src"
poetry run uvicorn main:app --reload --host 127.0.0.1 --port 8000
```
